# Copy to .env and fill locally. NEVER commit the real .env.
# These are only needed if you run scripts locally; the live automation uses Make connections.
OPENAI_API_KEY=
# Optional visual engine (pick one)
CANVA_CONNECT_TOKEN=
PLACID_API_TOKEN=
HIGGSFIELD_API_KEY=
